# Local-Order-Parameter-Adaptation

The code was developed and run in Visual Studio Code using Python 3.12.6.

## Repository structure

- `code/`: numerical simulation code for basin stability.  
  Running `basin_size_nb_change_bate.py` directly will generate the data used for Fig. 5(a).

- `fig/`: plotting scripts for generating the figures.
